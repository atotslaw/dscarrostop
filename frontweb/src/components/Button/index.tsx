import './styles.css';

const Button = () => {
  return (
    <div className="btn-container">
      <button className="btn">
        <h6>ver catálogo</h6>
      </button>
    </div>
  );
};

export default Button;
